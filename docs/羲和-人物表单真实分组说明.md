# 羲和人物表单真实分组说明

当前版本已将新增/编辑人物表单中的分组字段接入真实数据库分组。

## 页面入口

- 新增生日表单；
- 编辑人物表单。

## 当前能力

分组字段支持：

- 从当前账号已有分组中选择；
- 选择“新建分组…”后输入新分组名称；
- 保存人物时，如果分组不存在，后端会自动创建该分组；
- 保存成功后，前端会重新拉取数据库分组列表。

## 设计原则

既保持“选择已有分组”的规范性，也保留快速录入时直接创建新分组的灵活性。

## 后续计划

- 在人物表单里支持分组颜色展示；
- 支持多分组；
- 支持把“未分组”人物批量整理到指定分组。
